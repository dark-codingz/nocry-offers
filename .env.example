# Supabase Config (mesmas chaves do NoCry Finance)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Em produção, adicione:
# AUTH_COOKIE_DOMAIN=.theresnocry.com

# FunilSpy - Cache/Rate-limit Configuration
# Upstash Redis (opcional - para cache distribuído e rate limiting na Vercel)
# Obtenha em: https://console.upstash.com/
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
# TTL do cache em segundos (padrão: 3600 = 1 hora)
# CACHE_TTL=3600
# Rate limit por minuto (padrão: 10) - só funciona se Upstash estiver configurado
# RATE_LIMIT_PER_MINUTE=10

# FunilSpy - Dorks (Pesquisa Web) Configuration
# SerpAPI key (colocar na Vercel env para produção)
# ⚠️ NÃO COMITAR em repositórios públicos
SERPAPI_KEY=
# Tempo de cache em segundos (default 3600)
CACHE_TTL=3600




